type AuthPayload {
  token: String!
  hasRefreshToken: Boolean!
}

type Mutation {
  signup(email: String!, password: String!, name: String!): AuthPayload
  login(email: String!, password: String!): AuthPayload
  addRefreshToken(refreshToken: String!): AuthPayload
}

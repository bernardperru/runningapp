type Query {
  getUserInfo: User!
  getDistanceSum: Float!
}

type User {
  id: Int!
  email: String!
  name: String!
  password: String!
  refreshToken: String
  years: [Year]
  activities: [Activity]
}

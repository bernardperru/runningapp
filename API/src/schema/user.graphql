type Query {
  postUser(email: String!, password: String!, refreshToken: String!): String!
  getUser: User!
}

type User {
  email: String!
  password: String!
  refreshToken: String!
  activities: [Activity!]
}

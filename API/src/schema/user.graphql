type Query {
  getUserInfo: User!
  getDistanceSum: Float!
  getYears: [Year]!
}

type User {
  id: Int!
  email: String!
  name: String!
  password: String!
  refreshToken: String
  activities: [Activity]
  weeks: [Week]
}

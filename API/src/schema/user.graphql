type Query {
  addRefreshToken(refreshToken: String!): String
  getActivities: [Activity!]!
}

type User {
  id: Int!
  email: String!
  name: String!
  password: String!
  refreshToken: String
  activities: [Activity]
}

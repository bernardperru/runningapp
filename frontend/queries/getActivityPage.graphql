query GetActivityPage($input: ActivityPageInput!) {
	getActivityPage(input: $input) {
		count
		activities {
			id
			activityId
			distance
			elapsed_time
			start_date
			summary_polyline
			average_cadence
			average_heartrate
			average_pace
			zone
		}
	}
}
